---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nemo-guardrails
  labels:
    app: nemo-guardrails
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nemo-guardrails
  template:
    metadata:
      labels:
        app: nemo-guardrails
    spec:
      initContainers:
        - name: ensure-models
          image: quay.io/rh-ee-mmisiura/nemo-guardrails:no_models
          imagePullPolicy: Always
          command: ["/bin/bash", "-c"]
          args:
            - |
              set -e
              echo "Checking if models are available in PVC..."
              # Check if models are already downloaded
              if [ -d "/app/.cache/sentence_transformers" ] && \
                 [ -d "/app/.cache/transformers" ] && \
                 [ -d "/app/.cache/nltk_data" ]; then
                echo "✓ Models already present, skipping download"
                du -sh /app/.cache/*
                exit 0
              fi
              echo "Models not found, downloading..."
              /app/.venv/bin/python /app/scripts/pre_download_required_models.py
              echo "✓ All models downloaded successfully to PVC"
              du -sh /app/.cache/*
          env:
            - name: GUARDRAILS_PROFILE
              value: "opensource"
            - name: HF_HOME
              value: "/app/.cache/huggingface"
            - name: TRANSFORMERS_CACHE
              value: "/app/.cache/transformers"
            - name: SENTENCE_TRANSFORMERS_HOME
              value: "/app/.cache/sentence_transformers"
            - name: NLTK_DATA
              value: "/app/.cache/nltk_data"
            - name: FASTEMBED_CACHE_PATH
              value: "/app/.cache/fastembed"
          volumeMounts:
            - name: models-cache
              mountPath: /app/.cache
          resources:
            requests:
              memory: "2Gi"
              cpu: "1"
            limits:
              memory: "4Gi"
              cpu: "2"
      containers:
        - name: server
          image: quay.io/rh-ee-mmisiura/nemo-guardrails:no_models
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
              name: http
              protocol: TCP
          env:
            - name: CONFIG_ID
              value: "nemo"
            - name: PORT
              value: "8000"
            # Model cache paths
            - name: HF_HOME
              value: "/app/.cache/huggingface"
            - name: TRANSFORMERS_CACHE
              value: "/app/.cache/transformers"
            - name: SENTENCE_TRANSFORMERS_HOME
              value: "/app/.cache/sentence_transformers"
            - name: NLTK_DATA
              value: "/app/.cache/nltk_data"
            - name: FASTEMBED_CACHE_PATH
              value: "/app/.cache/fastembed"
          volumeMounts:
            - name: config
              mountPath: /app/config/nemo
              readOnly: true
            - name: models-cache
              mountPath: /app/.cache
          resources:
            requests:
              memory: "2Gi"
              cpu: "1"
            limits:
              memory: "4Gi"
              cpu: "2"
          livenessProbe:
            httpGet:
              path: /
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
      volumes:
        - name: config
          configMap:
            name: nemo-config
        - name: models-cache
          persistentVolumeClaim:
            claimName: nemo-models-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: nemo-guardrails-service
  labels:
    app: nemo-guardrails
spec:
  type: ClusterIP
  selector:
    app: nemo-guardrails
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: nemo-guardrails
  labels:
    app: nemo-guardrails
spec:
  to:
    kind: Service
    name: nemo-guardrails-service
  port:
    targetPort: http
