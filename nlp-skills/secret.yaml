apiVersion: v1
kind: Secret
metadata:
  name: llm-secret
type: Opaque
stringData:
  api-base: "${LLM_API_BASE}"
  model-name: "${LLM_MODEL_NAME}"
  api-key: "${LLM_API_KEY}"
  content-safety-api-base: "${CONTENT_SAFETY_API_BASE}"
  content-safety-model-name: "${CONTENT_SAFETY_MODEL_NAME}"
  content-safety-api-key: "${CONTENT_SAFETY_API_KEY}"
